<UserControl x:Class="Phexor_Task_Manager.MVVM.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
              mc:Ignorable="d"
              >
    <UserControl.Resources>
        <!-- Phoenix color palette (warm/orange/red/yellow/green accents) -->
        <Color x:Key="BackgroundColor">#FFFDF8F3</Color>
        <Color x:Key="CardColor">#FFFFFFFF</Color>
        <Color x:Key="ForegroundColor">#FF1D1D1B</Color>
        <Color x:Key="AccentColor">#FFEF6A00</Color>
        <Color x:Key="AccentRed">#FFCC2E2E</Color>
        <Color x:Key="AccentYellow">#FFFFD54F</Color>
        <Color x:Key="AccentGreen">#FF7ED957</Color>

        <SolidColorBrush x:Key="BackgroundBrush" Color="{DynamicResource BackgroundColor}"/>
        <SolidColorBrush x:Key="CardBrush" Color="{DynamicResource CardColor}"/>
        <SolidColorBrush x:Key="ForegroundBrush" Color="{DynamicResource ForegroundColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{DynamicResource AccentColor}"/>
        <SolidColorBrush x:Key="AccentRedBrush" Color="{DynamicResource AccentRed}"/>
        <SolidColorBrush x:Key="AccentYellowBrush" Color="{DynamicResource AccentYellow}"/>
        <SolidColorBrush x:Key="AccentGreenBrush" Color="{DynamicResource AccentGreen}"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="6,6"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#22000000" BlurRadius="6" ShadowDepth="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="TaskItemTemplate">
            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="160" />
                    </Grid.ColumnDefinitions>
                    <!-- Checkbox bound to a boolean property (e.g. IsDone) -->
                    <CheckBox Grid.Column="0" VerticalAlignment="Center" IsChecked="{Binding IsDone, Mode=TwoWay}"/>

                    <StackPanel Grid.Column="1" Margin="8,0,8,0">
                        <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding Description}" FontSize="12" Opacity="0.8" TextWrapping="Wrap"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="{Binding StartDate, StringFormat='Start: {0:d}'}" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding TargetDate, StringFormat='Ziel: {0:d}'}" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header / Toolbar -->
        <DockPanel Margin="12" Grid.Row="0">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <TextBlock Text="Aufgaben" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Text="  -  Übersicht" Margin="8,0,0,0" VerticalAlignment="Center" Opacity="0.7"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                <!-- Removed dark-mode toggle. Primary action uses phoenix accent -->
                <Button Content="Erstellen" Command="{Binding OpenCreateCommand}" Padding="10,4" Margin="0,0,8,0" Background="{DynamicResource AccentBrush}" Foreground="White"/>
            </StackPanel>
        </DockPanel>

        <!-- Main list area -->
        <Border Grid.Row="1" Margin="12" Background="Transparent">
            <ListView ItemsSource="{Binding Tasks}" ItemTemplate="{StaticResource TaskItemTemplate}" SelectedItem="{Binding SelectedTask, Mode=TwoWay}" Background="Transparent" BorderThickness="0">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding OpenChangeCommand}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <!-- Better spacing between items -->
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
        </Border>
    </Grid>
</UserControl>
