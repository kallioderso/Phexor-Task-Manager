<UserControl x:Class="Phexor_Task_Manager.MVVM.View.CreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              mc:Ignorable="d"
              >

    <UserControl.Resources>
        <!-- Phoenix color palette (warm/orange/red/yellow/green accents) -->
        <Color x:Key="BackgroundColor">#FFFDF8F3</Color>
        <Color x:Key="CardColor">#FFFFFFFF</Color>
        <Color x:Key="ForegroundColor">#FF1D1D1B</Color>
        <Color x:Key="AccentColor">#FFEF6A00</Color>
        <Color x:Key="AccentRed">#FFCC2E2E</Color>
        <Color x:Key="AccentYellow">#FFFFD54F</Color>
        <Color x:Key="AccentGreen">#FF7ED957</Color>

        <SolidColorBrush x:Key="BackgroundBrush" Color="{DynamicResource BackgroundColor}"/>
        <SolidColorBrush x:Key="CardBrush" Color="{DynamicResource CardColor}"/>
        <SolidColorBrush x:Key="ForegroundBrush" Color="{DynamicResource ForegroundColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{DynamicResource AccentColor}"/>
        <SolidColorBrush x:Key="AccentRedBrush" Color="{DynamicResource AccentRed}"/>
        <SolidColorBrush x:Key="AccentYellowBrush" Color="{DynamicResource AccentYellow}"/>
        <SolidColorBrush x:Key="AccentGreenBrush" Color="{DynamicResource AccentGreen}"/>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Opacity" Value="0.85"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="Control">
            <Setter Property="Margin" Value="0,2,0,10"/>
            <Setter Property="Padding" Value="6"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="12" Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Neue Aufgabe erstellen" FontSize="20" FontWeight="Bold"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Background="{DynamicResource CardBrush}" CornerRadius="8" Padding="12" Margin="0,12,0,12">
                <StackPanel Width="640" HorizontalAlignment="Left">
                    <TextBlock Text="Titel" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding Title, Mode=TwoWay}" Style="{StaticResource InputStyle}" />

                    <TextBlock Text="Beschreibung" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding Description, Mode=TwoWay}" AcceptsReturn="True" Height="120" TextWrapping="Wrap" Style="{StaticResource InputStyle}" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,6,0,0">
                        <StackPanel Width="300">
                            <TextBlock Text="Startdatum" Style="{StaticResource LabelStyle}"/>
                            <DatePicker SelectedDate="{Binding StartDate, Mode=TwoWay}"/>
                        </StackPanel>
                        <StackPanel Width="300" Margin="20,0,0,0">
                            <TextBlock Text="Zieldatum" Style="{StaticResource LabelStyle}"/>
                            <DatePicker SelectedDate="{Binding TargetDate, Mode=TwoWay}"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,8,0,0">
                        <CheckBox IsChecked="{Binding IsDone, Mode=TwoWay}" Content="Erledigt" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Abbrechen" Command="{Binding CancelCreateCommand}" Width="120" Margin="0,0,8,0"/>
            <Button Content="Erstellen" Command="{Binding CreateCommand}" Width="140" Background="{DynamicResource AccentBrush}" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>

