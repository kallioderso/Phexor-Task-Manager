<UserControl x:Class="Phexor_Task_Manager.MVVM.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              mc:Ignorable="d"
              >

    <UserControl.Resources>
        <!-- Phoenix palette for settings preview -->
        <Color x:Key="BackgroundColor">#FFFDF8F3</Color>
        <Color x:Key="CardColor">#FFFFFFFF</Color>
        <Color x:Key="ForegroundColor">#FF1D1D1B</Color>
        <Color x:Key="AccentColor">#FFEF6A00</Color>

        <SolidColorBrush x:Key="BackgroundBrush" Color="{DynamicResource BackgroundColor}"/>
        <SolidColorBrush x:Key="CardBrush" Color="{DynamicResource CardColor}"/>
        <SolidColorBrush x:Key="ForegroundBrush" Color="{DynamicResource ForegroundColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{DynamicResource AccentColor}"/>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,8,0,6"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="12">
            <TextBlock Text="Einstellungen" FontSize="20" FontWeight="Bold"/>

            <TextBlock Text="Design" Style="{StaticResource SectionTitle}"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Dark mode removed; settings simplified -->
            </StackPanel>

            <TextBlock Text="Logging" Style="{StaticResource SectionTitle}"/>
            <ComboBox SelectedItem="{Binding SelectedLoggingLevel, Mode=TwoWay}" Width="200">
                <ComboBoxItem>None</ComboBoxItem>
                <ComboBoxItem>Error</ComboBoxItem>
                <ComboBoxItem>Info</ComboBoxItem>
                <ComboBoxItem>Debug</ComboBoxItem>
            </ComboBox>

            <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                <Button Content="Speichern" Command="{Binding SaveSettingsCommand}" Width="120" Background="{DynamicResource AccentBrush}" Foreground="White"/>
                <Button Content="Zurücksetzen" Command="{Binding ResetSettingsCommand}" Width="120" Margin="8,0,0,0"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Column="1" Background="Transparent" Padding="12">
            <StackPanel>
                <TextBlock Text="Vorschau" FontSize="16" FontWeight="SemiBold"/>
                <Border CornerRadius="8" Padding="12" Margin="0,8,0,0" Background="{DynamicResource CardBrush}">
                    <StackPanel>
                        <TextBlock Text="Beispiel einer Aufgabe" FontWeight="Bold"/>
                        <TextBlock Text="Beschreibungstext und Datumsvorschau" Opacity="0.8"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
